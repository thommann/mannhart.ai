{
  "name": "chatbot-server",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir=dist"
  },
  "dependencies": {
    "openai": "^6.45.0"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
