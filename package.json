{
  "name": "mannhart.ai",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "dev:mock": "npx @11ty/eleventy && node chatbot-server/mock-server.js",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pdfkit": "^0.17.2"
  }
}
